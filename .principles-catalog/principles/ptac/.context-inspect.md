# .principles inspect context — ptac
# Machine-executable pre-scan patterns for /dot-audit Phase 5.

### PTAC-NO-GENERATED-BLOBS

- `find $TARGET -name "*.jar" -o -name "*.exe" -o -name "*.wasm" | head -20` | MEDIUM | Binary executables committed to repo
- `find $TARGET -name "*.class" | head -20` | MEDIUM | Compiled Java class files committed
- `find $TARGET \( -name "*.pkl" -o -name "*.ser" \) | head -20` | LOW | Binary serialization files committed
