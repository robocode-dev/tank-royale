$id: #skipped-turn-event.yaml
$schema: http://json-schema.org/2020-12/schema#
description: Event occurring when a bot has skipped a turn, meaning that no intent has reached the server for a specific turn
extends:
  $ref: event.yaml