$id: #round-started-event.yaml
$schema: http://json-schema.org/2020-12/schema#
description: Event occurring when a new round has started.
extends:
  $ref: message.yaml
properties:
  roundNumber:
    description: The current round number in the battle when event occurred
    type: integer
required:
  - roundNumber