package dev.robocode.tankroyale.gui.player

/**
 * Defines the features that a battle player can support.
 * Different player implementations (live vs replay) support different feature sets.
 */
enum class BattlePlayerFeature {
     /** Seek to specific position in battle timeline */
    SEEK
}
