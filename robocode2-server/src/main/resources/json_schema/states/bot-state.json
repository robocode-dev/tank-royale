{
	"id": "#bot-state.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "description": "Current state of a bot",

	"type": "object",
	"extends": {
		"$ref": "bot-state-without-id.json"
	},
    "properties": {
        "id": {
            "description": "Unique identifier of bot for the observer",
            "type": "integer"
        }
	},
	"required": [
		"id"
	]
}