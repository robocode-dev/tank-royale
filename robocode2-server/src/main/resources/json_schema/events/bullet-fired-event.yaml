$id: #bullet-fired-event.yaml
$schema: http://json-schema.org/draft-07/schema#
description: Bot event occurring, when a bullet is fired
extends:
  $ref: event.yaml
properties:
  bullet:
    description: Bullet that was fired
    $ref: bullet-state.yaml
required:
- bullet