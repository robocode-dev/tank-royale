{
    "id": "#bot-ready.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "description": "Ready message from a bot to the game server",
    "type": "object",
    "properties": {
        "message-type": {
            "enum": [ "bot-ready" ]
        }
    },
    "required": [ "message-type" ]
}