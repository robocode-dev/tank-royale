id: #game-started-for-observer.yaml
$schema: http://json-schema.org/draft-06/schema#
description: New game info for an observer. New game has started.
extends:
  $ref: message.yaml
properties:
  gameSetup:
    description: Game setup
    $ref: ../game-setup.yaml
  participants:
    description: List of bots participating in this battle
    type: array
    items:
      $ref: ../participant.yaml
required:
- gameSetup
- participants